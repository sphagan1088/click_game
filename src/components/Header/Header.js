import React from "react";
import "./Header.css";

const Header = props => (
    <header className="header">
        <h1>Nintendo Clicker</h1>
        <h2>Click an image to start the game, the images will randomly shuffle, keep clicking images but don't click the same image or you lose the game!</h2>
    </header>
);

export default Header;